# タスクリスト

- `[x]` `src/constants/messages.ts` にDashboard用の文字列定数を追加する。
- `[x]` `src/components/DashboardTab.tsx` のハードコードされた文字列を `MESSAGES` を使って置き換える。
- `[x]` `src/components/DashboardTab.tsx` に `RadarChart` (recharts) を追加し、選択された最大5名の正規化スコアを描画する。
- `[x]` `src/components/ScoringTab.tsx` の `ScoreCell` コンポーネント内の `<input>` に `tabular-nums` クラスを追加して等幅フォントを適用する。
- `[x]` クライアントのビルドと動作確認を行い、型エラー等がないか検証する。
